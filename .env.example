# Anthropic API key for Claude LLM synthesis
ANTHROPIC_API_KEY=your_key_here

# Optional: override default Claude model
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# Optional: PostgreSQL for persistence (defaults to in-memory)
# DATABASE_URL=postgresql://user:pass@localhost/nyay
